# AjaxRequests
Send Request with basic technologies JS Vanilla, Fetch, Axios and jQuery

JS Vanilla
request.open("method","url" true)

Fetch
fetch(url)

Axios:
axios.method(url)

jQuery
 $.ajax({setting})
